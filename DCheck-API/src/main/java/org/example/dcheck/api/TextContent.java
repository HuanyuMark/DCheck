package org.example.dcheck.api;

/**
 * Date 2025/02/28
 * 其实现类的 {@link #getInputStream()} 读取的内容是文本
 * represent a text content, read as string form the input stream
 * @author 三石而立Sunsy
 */
public interface TextContent extends Content {
}
